version: '3.9'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - db_data:/app/backend/instance
    restart: unless-stopped

volumes:
  db_data:
